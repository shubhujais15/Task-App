<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900">
        <div class="p-10 text-white">
            <form action="/create" method="post">
                <input class="block w-full outline-none px-5 py-2 bg-zinc-800 rounded-lg" type="text" placeholder="Title" name="title">
                <textarea class="block w-full outline-none mt-2 px-5 py-2 resize-none bg-zinc-800 rounded-lg" placeholder="Write your task description" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Create Task">
            </form>
        </div>

        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if (files.length > 0) { %>
                <% files.forEach(val => { %>
                    <div class="min-w-72 px-3 py-4 bg-zinc-800 rounded-md text-white">
                        <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                        <a class="text-blue-500 inline-block mt-3" href="/files/<%= val %>">Read More.</a>
                    </div>
                    <% }) %>
            <% }else{ %>
                <h3 class="text-zinc-600">No Tasks Yet.</h3>
            <% } %>
            
        </div>
    </div>
</body>
</html>